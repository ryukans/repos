#include <stdio.h>
#define MAXSIZE
/*
void shellsort(
	int vector[],
	int size
);*/


int main()
{
	int v[MAXSIZE] = { 2, 37, 100, 9, 67 };
	int i;

	for (i = 0; i < MAXSIZE; i++)
		printf("%d", v[i]);

}